# Git 推送指南

## 方式一：使用自动化脚本（推荐）

### 步骤 1：在 GitHub 创建仓库

1. 登录 GitHub (https://github.com)
2. 点击右上角 **+** → **New repository**
3. 填写仓库信息：
   - **Repository name**: `user-auth-test`（或其他名称）
   - **Description**: `用户注册和登录集成测试项目`
   - **Public** 或 **Private**（选择公开或私有）
   - ⚠️ **不要勾选** "Initialize this repository with a README"
4. 点击 **Create repository**
5. 复制仓库地址（HTTPS 或 SSH）

**示例地址：**
- HTTPS: `https://github.com/your-username/user-auth-test.git`
- SSH: `git@github.com:your-username/user-auth-test.git`

### 步骤 2：运行推送脚本

双击运行 `git-push.bat` 文件，按照提示操作：

```
1. 脚本会自动初始化 Git 仓库
2. 添加所有文件到暂存区
3. 提示输入提交信息（可直接回车使用默认）
4. 提示输入远程仓库地址（粘贴步骤1复制的地址）
5. 自动推送到 GitHub
```

### 步骤 3：查看 GitHub Actions

推送成功后：
1. 打开 GitHub 仓库页面
2. 点击 **Actions** 标签
3. 查看 "用户认证集成测试" 工作流运行状态
4. 等待测试完成（约 2-3 分钟）

---

## 方式二：手动执行命令

### 在项目目录打开 PowerShell 或 CMD

```bash
cd "C:\Users\34445\Desktop\Software test"
```

### 执行以下命令

```bash
# 1. 初始化 Git 仓库
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit: 用户认证集成测试项目"

# 4. 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/your-username/user-auth-test.git

# 5. 设置主分支
git branch -M main

# 6. 推送
git push -u origin main
```

---

## 常见问题

### Q1: 推送时提示需要登录

**HTTPS 方式：**
- 输入 GitHub 用户名
- 输入 Personal Access Token（不是密码）

**如何获取 Token：**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. 勾选 `repo` 权限
4. 复制生成的 token（只显示一次）

**SSH 方式：**
- 需要先配置 SSH 密钥
- 参考：https://docs.github.com/en/authentication/connecting-to-github-with-ssh

### Q2: 推送失败 - "remote: Repository not found"

**原因：** 仓库地址错误或仓库不存在

**解决：**
1. 确认已在 GitHub 创建仓库
2. 检查仓库地址是否正确
3. 重新设置远程地址：
   ```bash
   git remote remove origin
   git remote add origin YOUR_CORRECT_URL
   git push -u origin main
   ```

### Q3: 推送失败 - "failed to push some refs"

**原因：** 远程仓库有本地没有的提交

**解决：**
```bash
# 先拉取远程更改
git pull origin main --allow-unrelated-histories

# 再推送
git push -u origin main
```

### Q4: 推送失败 - 网络问题

**解决：**
1. 检查网络连接
2. 尝试使用 VPN
3. 或使用 SSH 方式代替 HTTPS

### Q5: 如何查看远程仓库配置

```bash
git remote -v
```

### Q6: 如何修改远程仓库地址

```bash
git remote set-url origin NEW_URL
```

---

## 推送后的验证

### 1. 检查代码是否上传成功

访问 GitHub 仓库页面，确认文件已上传

### 2. 检查 GitHub Actions 是否运行

1. 点击 **Actions** 标签
2. 应该看到 "用户认证集成测试" 工作流正在运行
3. 等待完成（绿色✓表示成功）

### 3. 查看测试结果

点击工作流运行记录 → 查看 Summary：

**预期看到：**
```
📊 完整测试报告

测试统计
- 📝 总测试用例数: 15
- ✅ 通过: 15
- ❌ 失败: 0
- ⚠️ 错误: 0

✅ 测试状态: 全部通过
```

---

## 后续更新代码

当您修改代码后，使用以下命令推送更新：

```bash
# 1. 添加更改
git add .

# 2. 提交
git commit -m "描述你的更改"

# 3. 推送
git push
```

或者再次运行 `git-push.bat` 脚本

---

## 推送流程图

```
创建 GitHub 仓库
       ↓
复制仓库地址
       ↓
运行 git-push.bat
       ↓
输入仓库地址
       ↓
自动推送
       ↓
GitHub Actions 自动运行测试
       ↓
查看测试结果
```

---

## 需要帮助？

如果遇到问题：
1. 查看错误信息
2. 参考上面的常见问题
3. 检查 GitHub 仓库设置
4. 确认网络连接正常

---

**提示：** 首次推送可能需要输入 GitHub 凭据，建议使用 Personal Access Token 而不是密码。
