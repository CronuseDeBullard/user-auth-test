# 集成测试用例总览

## 📊 测试统计

| 测试模块 | 测试用例数 | 状态 |
|---------|-----------|------|
| 用户注册功能 | 9 | ✅ 已实现 |
| 用户登录功能 | 6 | ✅ 已实现 |
| 教师端功能 | 6 | ✅ 已实现 |
| 学生端功能 | 6 | ✅ 已实现 |
| 管理员端功能 | 6 | ✅ 已实现 |
| **总计** | **33** | **✅ 全部完成** |

---

## 1. 用户注册功能测试（9个用例）

**测试类：** `UserRegistrationIntegrationTest.java`

| 编号 | 测试用例 | 测试内容 | 预期结果 |
|------|---------|---------|---------|
| TC-REG-01 | 手机号已存在 | 使用已注册的手机号注册 | 返回400，提示已存在 |
| TC-REG-02 | 邮箱已存在 | 使用已注册的邮箱注册 | 返回400，提示已存在 |
| TC-REG-03 | 用户名已存在 | 使用已注册的用户名注册 | 返回400，提示已存在 |
| TC-REG-04 | 手机号长度≠11位 | 手机号不是11位 | 返回400，校验失败 |
| TC-REG-05 | 用户名长度<6位 | 用户名少于6位 | 返回400，校验失败 |
| TC-REG-06 | 用户名长度>24位 | 用户名超过24位 | 返回400，校验失败 |
| TC-REG-07 | 密码长度<6位 | 密码少于6位 | 返回400，校验失败 |
| TC-REG-08 | 密码长度>24位 | 密码超过24位 | 返回400，校验失败 |
| TC-REG-09 | 所有字段合法 | 所有字段符合要求 | 返回200，注册成功 |

---

## 2. 用户登录功能测试（6个用例）

**测试类：** `UserLoginIntegrationTest.java`

| 编号 | 测试用例 | 测试内容 | 预期结果 |
|------|---------|---------|---------|
| TC-LOGIN-01 | 正确的用户名和密码 | 使用正确凭据登录 | 返回200，登录成功 |
| TC-LOGIN-02 | 用户名不存在 | 使用不存在的用户名 | 返回401，登录失败 |
| TC-LOGIN-03 | 密码错误 | 使用错误的密码 | 返回401，登录失败 |
| TC-LOGIN-04 | 用户名为空 | 用户名字段为空 | 返回401，登录失败 |
| TC-LOGIN-05 | 密码为空 | 密码字段为空 | 返回401，登录失败 |
| TC-LOGIN-06 | 注册后立即登录 | 注册成功后立即登录 | 返回200，登录成功 |

---

## 3. 教师端功能测试（6个用例）

**测试类：** `TeacherIntegrationTest.java`

| 编号 | 测试用例 | 测试内容 | 预期结果 |
|------|---------|---------|---------|
| TC-TEACHER-01 | 创建考试 - 所有字段合法 | 创建考试，所有字段有效 | 返回200，创建成功 |
| TC-TEACHER-02 | 创建考试 - 标题为空 | 考试标题为空 | 返回400，创建失败 |
| TC-TEACHER-03 | 创建考试 - 时长≤0 | 考试时长小于等于0 | 返回400，创建失败 |
| TC-TEACHER-04 | 创建考试 - 总分≤0 | 考试总分小于等于0 | 返回400，创建失败 |
| TC-TEACHER-05 | 查询考试列表 - 按教师ID | 按教师ID查询考试 | 返回200，查询成功 |
| TC-TEACHER-06 | 查询考试列表 - 按科目 | 按科目查询考试 | 返回200，查询成功 |

---

## 4. 学生端功能测试（6个用例）

**测试类：** `StudentIntegrationTest.java`

| 编号 | 测试用例 | 测试内容 | 预期结果 |
|------|---------|---------|---------|
| TC-STUDENT-01 | 提交考试 - 成功提交 | 提交考试答案和成绩 | 返回200，提交成功 |
| TC-STUDENT-02 | 提交考试 - 考试不存在 | 提交不存在的考试 | 返回400，提交失败 |
| TC-STUDENT-03 | 查询考试记录 - 成功查询 | 查询学生的考试记录 | 返回200，查询成功 |
| TC-STUDENT-04 | 查询考试记录 - 无记录 | 查询无记录的学生 | 返回200，返回空列表 |
| TC-STUDENT-05 | 查询考试结果 - 成功查询 | 查询考试成绩和状态 | 返回200，查询成功 |
| TC-STUDENT-06 | 查询考试结果 - 记录不存在 | 查询不存在的考试记录 | 返回400，查询失败 |

---

## 5. 管理员端功能测试（6个用例）

**测试类：** `AdminIntegrationTest.java`

| 编号 | 测试用例 | 测试内容 | 预期结果 |
|------|---------|---------|---------|
| TC-ADMIN-01 | 查询所有用户 - 成功查询 | 查询系统所有用户 | 返回200，查询成功 |
| TC-ADMIN-02 | 按用户类型查询 - 查询教师 | 查询教师类型用户 | 返回200，只返回教师 |
| TC-ADMIN-03 | 按用户类型查询 - 查询学生 | 查询学生类型用户 | 返回200，只返回学生 |
| TC-ADMIN-04 | 删除用户 - 成功删除 | 删除指定用户 | 返回200，删除成功 |
| TC-ADMIN-05 | 删除用户 - 用户不存在 | 删除不存在的用户 | 返回400，删除失败 |
| TC-ADMIN-06 | 更新用户状态 - 成功更新 | 更新用户状态 | 返回200，更新成功 |

---

## 📁 测试文件结构

```
src/test/java/com/softwaretest/
├── UserRegistrationIntegrationTest.java  (9个用例)
├── UserLoginIntegrationTest.java         (6个用例)
├── TeacherIntegrationTest.java           (6个用例)
├── StudentIntegrationTest.java           (6个用例)
└── AdminIntegrationTest.java             (6个用例)
```

---

## 🎯 测试覆盖范围

### 功能覆盖
- ✅ 用户认证（注册、登录）
- ✅ 教师端（考试管理、查询）
- ✅ 学生端（考试提交、成绩查询）
- ✅ 管理员端（用户管理）

### 测试类型
- ✅ 正向测试（成功场景）
- ✅ 负向测试（失败场景）
- ✅ 边界值测试
- ✅ 数据校验测试
- ✅ 业务逻辑测试

---

## 🚀 运行测试

### 运行所有测试
```bash
mvn test
```

### 运行单个模块测试
```bash
# 用户注册测试
mvn test -Dtest=UserRegistrationIntegrationTest

# 用户登录测试
mvn test -Dtest=UserLoginIntegrationTest

# 教师端测试
mvn test -Dtest=TeacherIntegrationTest

# 学生端测试
mvn test -Dtest=StudentIntegrationTest

# 管理员端测试
mvn test -Dtest=AdminIntegrationTest
```

---

## 📊 GitHub Actions 自动化测试

工作流包含 **7 个任务**：

1. **test-user-registration** - 用户注册功能测试
2. **test-user-login** - 用户登录功能测试
3. **test-teacher** - 教师端功能测试
4. **test-student** - 学生端功能测试
5. **test-admin** - 管理员端功能测试
6. **test-all** - 完整测试套件
7. **test-summary** - 测试结果汇总

---

## 📈 测试进度

| 阶段 | 状态 | 完成度 |
|------|------|--------|
| 测试设计 | ✅ 完成 | 100% |
| 测试实现 | ✅ 完成 | 100% |
| CI/CD配置 | ✅ 完成 | 100% |
| 文档编写 | ✅ 完成 | 100% |

---

**最后更新：** 2026年6月1日  
**总测试用例数：** 33个  
**测试状态：** ✅ 全部完成
